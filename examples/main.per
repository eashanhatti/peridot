import c_compiler.per

metadef f: CDecl =
	func(Int, "plus", cons(pair(Int, "x"), cons(pair(Int, "y"), nil)),
		return(add(var("x"), var("y"))))

metavar t: Text
query CompileDecl(f, t)
output OUTPUT.c t
