import c_compiler.per
import p_compiler.per

metadef code: Code(Struct {}) = struct {}

metavar tm: CTerm
query CompilePTerm(<struct {}>, cons(pair(sigma(<Struct {}>, <struct {}>), "x"), nil), tm, nil)
