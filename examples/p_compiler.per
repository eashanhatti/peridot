import c_compiler.per
import list.per
import pair.per
import sigma.per

prop CompilePTerm
	( Code(`A)
	, List(Pair(Sigma(Code(Type), metafun(t) => Code(t)), Text))
	, CTerm
	, List(CDecl))
axiom compile_var_p:
	CompilePTerm(`x, `ctx, var(`t), `d)
	:- Lookup(`ctx, `x, `t)
