T : Type
T = record where
	fst : Fin 1
	snd : Fin 1
end

Pair : Type
Pair = record where
	fst : import main.T
	snd : Fin 2
end

foo : Fin 2
foo =
	match (record [ fst = (record [ fst = fin 0, snd = fin 0 ] : (import main.T)), snd = fin 0 ] : (import main.Pair)) with
		[[a, b], c] => a
	end