T : Type
T = record where
	fst : Fin 2
	snd : Fin 2
end

Pair : Type
Pair = record where
	fst : import main.T
	snd : Fin 2
end

foo : Fin 2
foo =
	match (record [ fst = fin 0, snd = fin 0 ] : (import main.T)) with
		[x, fin 0] => x
		[fin 1, y] => y
		[fin 0, z] => z
	end