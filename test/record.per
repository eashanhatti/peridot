let {
	'val Ty : OType =
		signature {
			T : OType,
			f : '[_ : T] -> T
		};

	'val record : Ty =
		structure {
			T = Bool,
			f = '\b { b }
		};

	'val field : '[_ : (#T record)] -> (#T record) = (#f record);
} in {
	OType
}
