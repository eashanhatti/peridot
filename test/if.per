/*
Dependent function types. The return type of `example` depends on the value of the parameter
*/
def example
  : Fun(b: Bool) ->
      if b {
        Bool
      } else {
        Equal(true, true)
      }
  = fun(b) =>
      if b {
        true
      } else {
        refl
      }
